// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  // boshqa sozlamalaring bo‘lsa qoldir
  experimental: {
    serverActions: {},
  },
};

module.exports = nextConfig;
